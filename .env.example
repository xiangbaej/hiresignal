# Postgres (Supabase 또는 로컬). 세션 풀러가 아닌 direct connection 권장.
DATABASE_URL=postgresql://postgres:password@localhost:5432/hiresignal

# 스캔 튜닝
SCAN_CONCURRENCY=6
SCAN_TIMEOUT_MS=20000

# 콜드메일 생성(유료 기능)에만 사용. 일일 스캔에는 필요 없음.
OPENAI_API_KEY=
